torch>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
transformers>=4.30.0
# pathway # Commented out if not strictly needed to avoid heavy deps like pdf2image
modal>=0.50.0 
pytest
pdf2image # Added just in case you do need pathway elsewhere
